#include <algorithm>
#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    int m;
    cin >> m;
    int arr[m][2];
    for (int i = 0; i < m; i++)
    {
        cin >> arr[i][0] >> arr[i][1];
    }
    sort(arr, arr
    return 0;
}
