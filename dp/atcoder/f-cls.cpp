#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

void solve(){
    string s, t; cin >> s >> t;
    int n = min(s.size(), t.size());
    vector<string> dp(n);
    

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
}
